# Builder

## Usage

```java
User user = new User.Builder("John", "Doe")
                .age(30)
                .phone("1234567")
                .address("Address 1234")
                .build();
```